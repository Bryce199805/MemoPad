services:
  backend:
    build: ./backend
    container_name: memopad-backend
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  web:
    build: ./web
    container_name: memopad-web
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
