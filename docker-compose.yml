version: '3.8'

services:
  backend:
    build: ./backend
    container_name: memo-desk-backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
